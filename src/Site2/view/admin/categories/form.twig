{% extends "layout.twig" %}

{% block content %}
 <div class="admin-container">
        {% include 'admin/menu.twig' with {'current_page': 'categories'} %}
        
        <div class="admin-content">
    
    <h1>{{ item ? 'Modifier' : 'Ajouter' }} une catégorie</h1>
    <form method="post" class="admin-form">
        <div class="form-group">
            <label for="name">Nom</label>
            <input type="text" id="name" name="name" value="{{ item.name|default('') }}" required>
        </div>
        <button type="submit" class="btn btn-primary">{{ item ? 'Modifier' : 'Ajouter' }}</button>
    </form>
    </div>
    </div>
{% endblock %}