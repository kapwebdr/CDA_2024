{% extends "layout.twig" %}

{% block content %}
    <div class="error-container">
        <h2>{{ errorType }}</h2>
        <p>Code d'erreur : {{ errorCode }}</p>
        <p>{{ errorMessage }}</p>
        <a href="/" class="btn-blue">Retour à l'accueil</a>
    </div>
{% endblock %}
